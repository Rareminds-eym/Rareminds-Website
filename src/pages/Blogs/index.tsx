import BlogListing from "@/components/Govt/Blogs/BlogListing";

const Blogs: React.FC = () => {
  // BlogListing should use Link or navigate to /blogs/:slug when a blog is selected
  return <BlogListing />;
};

export default Blogs;
